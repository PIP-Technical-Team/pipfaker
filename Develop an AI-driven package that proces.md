Develop an AI-driven package that processes resources from the directory \\wbgmsddg001\PIP\pipapi_data\ to generate a simplified or synthetic version of PIP data. The package must handle a variety of dataset types organized within each release folder.

Requirements and Workflow
Step 1: Enable the AI to select a release folder from \\wbgmsddg001\PIP\pipapi_data\ for processing.
Step 2: For datasets located in the _aux and estimations subfolders, as well as any files found in the root of the selected release folder, instruct the AI to copy them directly to the output location with no modifications except cache.duckdb which should not be coped.
Step 3: For datasets within the survey_data and lineup_data folders, require the AI to generate synthetic versions using the FakeDataR package, adhering to the following:
- Any files in the survey_data folder whose names end with GROUP or BIN should not be synthesized; instead, copy these files directly to the output as they are.
- For all other files in the survey_data and lineup_data folders, we need to use two files that would guide the synthics generation process:
a) we use "survey_means.qs" file to generate synthetic data for datasets in the survey_data folder. This file contains the mean values for each countnry per year and reporting level. The mean of the synthetic data should be based on these values to ensure that the generated data maintains a realistic distribution.
b) we use "interpolated_means.qs" file to generate synthetic data for datasets in the lineup_data folder. This file contains interpolated mean values for each country per year and reporting level. Similar to the survey_data, the mean of the synthetic data for lineup_data should be based on these interpolated values to ensure realism in the generated datasets.
c) How to idenitify and link a mean of a country per year and reporting level to the corresponding dataset in the survey_data and lineup_data folders. There are two variables in the survey_means.qs and interpolated_means.qs files that can be identify the datasets in the survey_data and lineup_data folders: the three digit country code (iso3c) and the year values in "country_code" and "reporting_year" variables respectively can be used to identify datasets in the survey_data and lineup_data folders. As the dataset names start with three digit country code followed by underscore and reporting year, the AI can use these variables to match the datasets with their corresponding mean values for synthetic data generation. For example, if there is a dataset named "USA_2020.csv" in the survey_data folder, the AI can look for the mean values in the survey_means.qs file where the country_code is "USA" and reporting_year is "2020" to generate the synthetic version of that dataset.
d) Ensure that the synthetic data generated for both survey_data and lineup_data maintains the same structure (i.e., same columns and data types) as the original datasets to ensure compatibility with downstream processes that rely on this data.
Step 4: After processing, the AI should save all the copied and synthesized datasets to a specified output location, maintaining the same folder structure as the original release folder for easy navigation and access. The output location should be configurable to allow users to specify where they want the processed data to be stored.
Step 5: Implement error handling to manage any issues that arise during the processing, such as missing files, mismatched mean values, or issues with the FakeDataR package. The AI should log any errors encountered and provide informative messages to help users understand what went wrong and how to fix it.
Step 6: Provide documentation and usage instructions for the package, including how to install it, how to configure the output location, and how to run the processing workflow. Include examples of how to use the package with sample data to demonstrate its functionality.
Step 7: Refractor the code to ensure it is efficient, maintainable, and follows best practices for R programming. This includes organizing the code into functions, using appropriate data structures, and ensuring that the code is well-commented for clarity.
Step 8: Test the package with various datasets to ensure that it works correctly and produces the expected results.
